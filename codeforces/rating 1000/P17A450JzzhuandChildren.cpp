#include<bits/stdc++.h>
using namespace std;
#define ll                  long long
#define ull                 unsigned long long LL
#define md                  10000007
#define nline "\n"
void solve() {
	int n, m;
	vector<int>v1;
	vector<int>v2;
	for (int i = 1; i <= n; i++) {
		int x;
		cin >> x;
		v1.push_back(x);
		v2.push_back(i);
	}

}
int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	int t = 1;
	//cin >> t;
	for (int i = 1; i <= t; i++) {
		//cout<<"Case "<<i<<": ";
		solve();
	}
	return 0;
}